<!-- Blind reviewer fixture. Audience: first-year undergraduate, first exposure to AC circuits and to differential equations. Review this fragment as an assembled Phase 3 topic body; report issues with location "R10". -->

```jsx
<Section title="Continuity of stored-energy variables">
  <P>
    The voltage across a capacitor cannot change instantaneously, because <M>{"i = C\\,dv/dt"}</M> and a
    finite current cannot produce an infinite <M>{"dv/dt"}</M>; the energy stored in a capacitor is
    <M>{"\\tfrac{1}{2}Cv^2"}</M>, and the current through an inductor likewise cannot change
    instantaneously, since <M>{"v = L\\,di/dt"}</M> would require an infinite voltage, while the inductor's
    stored energy is <M>{"\\tfrac{1}{2}Li^2"}</M>, so at a switching instant <M>{"t = 0"}</M> the values
    <M>{"v_C(0^-) = v_C(0^+)"}</M> and <M>{"i_L(0^-) = i_L(0^+)"}</M> carry over, which is what lets a
    first-order transient be solved from a single initial condition, and both energies are what the circuit
    dissipates in the resistor afterwards.
  </P>
</Section>
```
