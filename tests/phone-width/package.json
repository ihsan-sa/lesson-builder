{
  "name": "phone-width-tests",
  "private": true,
  "description": "Playwright evidence that at 390px no equation's controls overlap the equation and no part of the lesson is squeezed to nothing, in both stylesheets. Run: ./run.sh",
  "scripts": { "test": "./run.sh" },
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
