{
  "name": "resume-metadata-tests",
  "private": true,
  "description": "Playwright evidence that resuming a chat session restores its model/effort. Run: ./run.sh",
  "scripts": { "test": "./run.sh" },
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
